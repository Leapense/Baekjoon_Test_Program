# Baekjoon Test Program

This repository contains a GUI application for running and testing source code for the Baekjoon Online Judge.

## Requirements

Install Python 3 and the packages listed in `requirements.txt`:

```bash
pip install -r requirements.txt
```

## Usage

Run the application with:

```bash
python main.py
```
